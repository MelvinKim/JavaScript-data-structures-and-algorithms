/*
    input => two arrays
    output => one merged array
    examples => [0,1,2] [-1,6,8] => [-1,0, 1,2,6,8]
    corner cases => if the array is empty (don't perform any swap)

    loop over both of the arrays
    compare 
    swap
*/

var mergeArrays = (nums1, m, nums2, n) => {

	let first = m - 1;
	let second = n - 1;

    
};

console.log(mergeArrays([1, 6, 3], 3, [2, 5, 6], 3));
